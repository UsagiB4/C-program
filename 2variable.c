# include <stdio.h>
/*
this is a multiple line comment.
*/

int main(){
    int a_a = 12;  // This is an intiger
    char b = 'h';  // This is a character
    char name[] = "Mark"; // This is an array
    float _fl = 23.2;  // This is a floating number
    int aNum;
    printf("Hello %s \n", name);
    printf("%d is not equal to %f \n", a_a, _fl);
    printf("%cello world", b);

    return 0;
}